version: "3"
services:
  react:
    image: node:12.17.0
    working_dir: /code
    volumes:
      - .:/code
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
    command: >
      bash -c "cd react_app &&
      yarn start"
    container_name: react_yaruzo_node